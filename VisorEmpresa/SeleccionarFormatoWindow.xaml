<Window x:Class="VisorEmpresa.SeleccionarFormatoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Crear Plantilla"
        Width="360" Height="210"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Background="{DynamicResource ThemeBgPrincipal}">

    <Window.Resources>
        <Style x:Key="BtnFormato" TargetType="Button">
            <Setter Property="Width"           Value="140"/>
            <Setter Property="Height"          Value="70"/>
            <Setter Property="Foreground"      Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.88"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.72"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="¿En qué formato querés crear la plantilla?"
                       FontSize="13" TextWrapping="Wrap" TextAlignment="Center"
                       Foreground="{DynamicResource ThemeTexto}"
                       HorizontalAlignment="Center" Margin="0,0,0,20"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="BtnExcel" Background="#1E7D32"
                        Style="{StaticResource BtnFormato}" Margin="0,0,14,0"
                        Click="BtnExcel_Click">
                    <StackPanel>
                        <TextBlock Text="📊" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="Excel" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                    </StackPanel>
                </Button>
                <Button x:Name="BtnPdf" Background="#B71C1C"
                        Style="{StaticResource BtnFormato}"
                        Click="BtnPdf_Click">
                    <StackPanel>
                        <TextBlock Text="📄" FontSize="22" HorizontalAlignment="Center"/>
                        <TextBlock Text="PDF" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
